
# electron-builder walks node_modules as a real tree; pnpm symlinks break its dependency
# scan (ENOENT: scandir .../node_modules/deferred-leveldown) and the packaged app ends up
# missing modules. A hoisted layout is what it expects.
node-linker=hoisted
